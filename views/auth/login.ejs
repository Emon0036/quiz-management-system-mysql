<% layout('layouts/main') %>

<section class="auth-screen">
  <div class="auth-stage animate-fade-in">
    <aside class="auth-showcase">
      <div class="auth-showcase__content">
        <a href="/" class="auth-brand">
          <span class="auth-brand__mark">
            <i class="fa-brands fa-joomla"></i>
          </span>
          <span class="auth-brand__copy">
            <strong><%= appName %></strong>
            <small>Assessment made clearer</small>
          </span>
        </a>

        <div class="auth-showcase__intro">
          <span class="auth-kicker">QuizMaster access</span>
          <h1>Ready to continue?</h1>
          <p>Return to your quizzes, results, and review work.</p>
        </div>

        <div class="auth-preview-card" aria-hidden="true">
          <div class="auth-preview-card__header">
            <span class="auth-preview-card__icon"><i class="fas fa-layer-group"></i></span>
            <strong>Dashboard</strong>
          </div>
          <div class="auth-preview-list">
            <span><i class="fas fa-clipboard-list"></i> Quizzes</span>
            <span><i class="fas fa-chart-line"></i> Scores</span>
            <span><i class="fas fa-clipboard-check"></i> Reviews</span>
          </div>
        </div>
      </div>

      <div class="auth-showcase__footer">
        <div class="auth-mini-strip">
          <span><i class="fas fa-shield-halved"></i> Secure access</span>
          <span><i class="fas fa-user-check"></i> Role based</span>
        </div>
      </div>
    </aside>

    <section class="auth-form-panel">
      <div class="auth-form-card">
        <a href="/" class="auth-brand auth-brand--mobile">
          <span class="auth-brand__mark">
            <i class="fa-brands fa-joomla"></i>
          </span>
          <span class="auth-brand__copy auth-brand__copy--dark">
            <strong><%= appName %></strong>
            <small>Assessment made clearer</small>
          </span>
        </a>

        <div class="auth-form-header">
          <h2>Sign in</h2>
          <p>Use your email and password.</p>
        </div>

        <form action="/auth/login" method="POST" class="auth-form-stack" x-data="{ showPassword: false }">
          <% if (currentTabId) { %>
            <input type="hidden" name="tab" value="<%= currentTabId %>">
          <% } %>

          <label class="auth-field">
            <span>Email address</span>
            <input
              id="email"
              name="email"
              type="email"
              required
              class="form-input"
              placeholder="Enter your email"
              value="<%= typeof email !== 'undefined' ? email : '' %>"
            >
          </label>

          <label class="auth-field">
            <span>Password</span>
            <div class="auth-input-wrap">
              <input
                id="password"
                name="password"
                :type="showPassword ? 'text' : 'password'"
                required
                class="form-input auth-input-pad"
                placeholder="Enter your password"
              >
              <button
                type="button"
                @click="showPassword = !showPassword"
                class="auth-input-toggle"
                aria-label="Toggle password visibility"
              >
                <i :class="showPassword ? 'fas fa-eye-slash' : 'fas fa-eye'"></i>
              </button>
            </div>
          </label>

          <div class="auth-inline-row">
            <label for="remember" class="auth-check">
              <input
                id="remember"
                name="remember"
                type="checkbox"
                class="form-check-input"
              >
              <span>Remember me</span>
            </label>

            <a href="/auth/forgot-password" class="auth-subtle-link">Forgot password?</a>
          </div>

          <button type="submit" class="btn btn-primary auth-submit">
            <i class="fas fa-right-to-bracket"></i>
            <span>Sign in</span>
          </button>
        </form>

        <p class="auth-alt-copy">
          Don't have an account?
          <a href="/auth/register" class="auth-subtle-link">Sign up</a>
        </p>

        <% if (process.env.NODE_ENV === 'development') { %>
          <details class="auth-dev-credentials">
            <summary>Demo credentials</summary>
            <p><strong>Admin:</strong> admin@example.com / admin123</p>
            <p><strong>Teacher:</strong> teacher@example.com / teacher123</p>
            <p><strong>Student:</strong> student@example.com / student123</p>
          </details>
        <% } %>
      </div>
    </section>
  </div>
</section>
